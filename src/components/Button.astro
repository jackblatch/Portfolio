---
export interface Props {
  text: string;
  link: string;
}

const { text, link } = Astro.props;
---

<a
  href={link}
  class="inline-block text-sm border border-ink-line dark:border-night-line px-4 py-2 rounded hover:border-ink-fg dark:hover:border-night-fg transition-colors"
>
  {text}
</a>
