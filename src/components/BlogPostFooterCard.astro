---
export interface Props {
  href: string;
  text: string;
}

const { text, href } = Astro.props;
---

<a
  href={href}
  class="no-underline text-sm text-ink-muted dark:text-night-muted hover:text-accent transition-colors"
>
  &larr; {text}
</a>
